{
  "name": "@iii/app-studio",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Virtual app-studio orchestrated on iii (P0). See app-studio/DESIGN.md.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
