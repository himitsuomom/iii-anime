# Written by .github/scripts/pin_docs.py with Python json.dumps; keep
# formatters from re-collapsing its arrays and causing format ping-pong.
docs/docs.json

# Generated by scripts/generate-cli-docs.sh from the clap CLI definitions;
# CI diffs a fresh regeneration against the committed files, so formatter
# rewrites would read as drift. (Matches version-rotated copies too.)
docs/*/cli-reference/

# Vendored third-party Claude Code resources imported verbatim by
# scripts/import-community-skills.py; keep them as-is and free of format churn.
community-skills/
